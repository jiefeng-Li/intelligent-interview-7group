spring:
  application:
    name: service-job
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/db_job
    username: root
    password: 3121
  cloud:
    compatibility-verifier:
      enabled: false
server:
  port: 10008
